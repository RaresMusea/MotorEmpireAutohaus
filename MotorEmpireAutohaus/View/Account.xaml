<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MotorEmpireAutohaus.View.Account"
             xmlns:viewmodel="clr-namespace:MotorEmpireAutohaus.View_Model.Account"
             x:DataType="viewmodel:UserAccountViewModel"
             Title="My Account">
    <ScrollView>
        <VerticalStackLayout>
            <BoxView BackgroundColor="{AppThemeBinding Dark=Grey, Light=#1C6758}"
                     Color="{AppThemeBinding Dark=Grey, Light=#1C6758}"
                     HeightRequest="300"
                     CornerRadius="25"
>
            </BoxView>
    </VerticalStackLayout>
    </ScrollView>
</ContentPage>